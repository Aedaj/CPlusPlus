#include <iostream> //Header file that contains function for basic input and output operations

//The main function
int main(){
    std::cout << "I like ramen!" << std::endl;
    // Standard :: char output -> << <- means output
    std::cout << "It's really good!" << '\n'; 
    return 0; // Goes at the end of main function. If we reach return 0, there were no problems in this program. 
}